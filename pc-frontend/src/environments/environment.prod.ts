// Production build (Render static site).
// Replace the placeholder below with your Render backend service URL
// BEFORE running `npm run build` for deployment. Example:
//   apiBaseUrl: 'https://pharmaconnect-backend.onrender.com'
//
// This file is substituted in for environment.ts at build time via the
// `fileReplacements` block in angular.json's production configuration.
export const environment = {
  apiBaseUrl: 'https://pharmaconnect-backend-sl7k.onrender.com',
};
